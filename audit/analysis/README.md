<!-- dev-component: id=audit-analysis kind=subcomponent group=audit desc="Analysis engines used by the audit component" -->

# analysis

Analysis engines for the `audit` component.

- `zeek_snapshot.py`: parses Zeek logs and emits JSON, summary, markdown, and graph artifacts.
