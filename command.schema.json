{
  "name": "dev_command_contract",
  "object": "command",
  "definition": "canonical public command object used by legend, index, and dashboard display.",
  "required": {
    "alias": "string; compact invocation token shown in human-facing views",
    "name": "string; terse no-space/no-dot capability identifier",
    "path": "string; repo-relative implementation location",
    "path_key": "string; stable repo-path reference formatted as path#alias",
    "ref": "string; stable taxonomy-style reference formatted as scope/component/group/name",
    "run": "user|sudo; execution privilege expectation",
    "desc": "string; concise summary",
    "group": "sys|audit|net; fixed simple command category"
  },
  "rules": [
    "commands are the invokable subset of discovered records",
    "alias is required for human-facing command views and public aliases should be <= 4 chars",
    "command names are terse identifiers, not formal labels",
    "public command names should be <= 12 chars and descriptions should be compact",
    "command aliases and names avoid dot notation",
    "public command objects expose alias, name, path, path_key, ref, run, desc, and group",
    "group must be one of sys, audit, or net"
  ]
}
