# bootstrap

Environment bootstrap and publication helpers.

- `zsh/`: tracked shell profile source (`bootstrap/zsh/.zshrc`)
- `bin/`: publish/sync helpers for local dotfiles
- `git/`: git profile/template helpers
