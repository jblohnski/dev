# Operations Index

Rough file-by-file operation map inferred from script metadata headers.

- `audit/audit.sh`: Quick macOS audit with network/process focus + lightweight delta (`run=user`, `tags=audit macos network process`)
- `audit/collectors/binaries.sh`: Collect executable inventory and suspicious unsigned binaries (`run=user`, `tags=audit collector binaries signing`)
- `audit/collectors/extensions.sh`: Collect kernel/system extension listings (`run=user`, `tags=audit collector extensions`)
- `audit/collectors/launch_nonapple.sh`: Collect non-Apple launch agents and daemons (`run=user`, `tags=audit collector launch persistence`)
- `audit/collectors/launchctl.sh`: Dump launchctl system and GUI trees (`run=user`, `tags=audit collector launchctl persistence`)
- `audit/collectors/net_live.sh`: Capture live socket and route snapshot (`run=user`, `tags=audit collector network live`)
- `audit/collectors/net_path.sh`: Capture default route, DNS, and interfaces (`run=user`, `tags=audit collector network path dns`)
- `audit/collectors/network.sh`: Capture active network connections via lsof (`run=user`, `tags=audit collector network lsof`)
- `audit/collectors/network_deep.sh`: Run deep network audit baseline/compare flow (`run=sudo`, `tags=audit collector network deep`)
- `audit/collectors/persistence.sh`: Collect persistence surfaces and login items (`run=user`, `tags=audit collector persistence launch`)
- `audit/collectors/proc_watch.sh`: Burst-sample process table for short-lived processes (`run=user`, `tags=audit collector process watch`)
- `audit/collectors/processes.sh`: Capture sorted process list snapshot (`run=user`, `tags=audit collector process snapshot`)
- `audit/collectors/signing.sh`: Verify signatures for core system binaries (`run=user`, `tags=audit collector signing integrity`)
- `audit/collectors/trust.sh`: Collect trust chain and SIP-related anomaly signals (`run=user`, `tags=audit collector trust sip`)
- `audit/macsm-anlyz.sh`: Analyze macsm watch run artifacts and summarize diffs (`run=user`, `tags=audit macsm analysis`)
- `audit/macsm.sh`: Watch macOS accessibility/audio preference changes (`run=user`, `tags=audit macsm watcher`)
- `audit/zeek-audit.sh`: Run Zeek correlation report generation (`run=user`, `tags=audit zeek forensics`)
- `audit/zeek-capture.sh`: Start, stop, and inspect background Zeek capture writing logs to the configured log dir (`run=user`, `tags=audit zeek capture logs`)
- `audit/zeek-logsync.sh`: Merge local Zeek log files into the canonical configured log dir (`run=user`, `tags=audit zeek logs sync`)
- `ops/browser/firefox-harden.sh`: Apply arkenfox profile and Firefox hardening overrides (`run=user`, `tags=ops browser firefox hardening`)
- `ops/diagnostics/logsum.sh`: Summarize macOS unified logs for hangs/beachballs (system + Firefox) (`run=user`, `tags=diag logs macos`)
- `ops/diagnostics/wstrace.sh`: Trace WindowServer-related faults/errors for UI stutter and beachballs (`run=user`, `tags=diag logs ui macos`)
- `ops/network/router.sh`: Audit router/Wi-Fi security posture and exposure (`run=user`, `tags=ops network security audit`)
- `ops/pf/bin/pfkit-apply.sh`: Render and load pfkit anchor rules (`run=sudo`, `tags=ops pf firewall apply`)
- `ops/pf/bin/pfkit-install.sh`: Install pfkit anchor and pf.conf wiring (`run=sudo`, `tags=ops pf firewall install`)
- `ops/pf/bin/pfkit-uninstall.sh`: Remove pfkit anchor and restore pf config (`run=sudo`, `tags=ops pf firewall uninstall`)
- `ops/pf/bin/pfkit-watch-blocks.sh`: Stream blocked packets from pflog0 (`run=sudo`, `tags=ops pf firewall monitor`)
- `ops/pf/bin/pfkit-watch-dns.sh`: Stream DNS packets from selected interface (`run=sudo`, `tags=ops pf dns monitor`)
- `ops/system/harden.sh`: Harden macOS security defaults (remote access, firewall, privacy) (`run=sudo`, `tags=sec system macos`)
